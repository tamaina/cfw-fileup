See [AGENTS.md](AGENTS.md) !

There is `.claude` directory.   
It contains `skills`, `agents` and `rules` symbolic links to `.agents/skills`, `.agents/agents` and `.agents/rules` respectively.

@AGENTS.md
