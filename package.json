{
  "name": "cfw-fileup",
  "type": "module",
  "scripts": {
    "dev": "pnpm run --filter app dev",
    "build": "pnpm run --filter app build",
    "deploy": "pnpm run --filter app deploy"
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
